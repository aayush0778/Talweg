# SlopeGuard AI — ML Service (FastAPI)
#
# NOT IMPLEMENTED YET — this is a Phase 5 scaffold.
# This service will host the trained Random Forest model behind /predict.
# Node/Express calls this internally; the browser never talks to it directly.
#
# Install: pip install -r requirements.txt
# Run:     uvicorn app.main:app --port 8000

fastapi>=0.115.0
uvicorn>=0.32.0
scikit-learn>=1.5.0
pandas>=2.2.0
joblib>=1.4.0
numpy>=1.26.0
